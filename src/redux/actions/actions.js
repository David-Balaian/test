export function SELECT_FILTER(payload){
    return {
        type: "SELECT_FILTER",
        payload,
    }
}

export function SELECT_PANEL(payload){
    return {
        type: "SELECT_PANEL",
        payload,
    }
}